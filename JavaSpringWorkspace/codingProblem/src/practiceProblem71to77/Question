
/**
 * 71. Concatenate and Convert: Take two strings, concatenate them, and convert
 * the result to uppercase.
 **/

/**
 * 72. Calculate the area and circumference of a circle for a given radius using
 * Math.PI.
 **/

/**
 * 73. Simulate a dice roll using Math.random() and display the outcome (1 to
 * 6).
 **/

/**
 * 74. Create a number guessing game where the program selects a random number,
 * and the user has to guess it.
 **/

/**
 * 75. Take an array of words and concatenate them into a single string using
 * StringBuilder.
 **/

/**
 * 76. Create an object with final fields and a constructor to initialize them.
 **/
